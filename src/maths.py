from .error_handling import *
from .matrix_operations import *

def compute_exp(args):
    args.pop(0)
    matrix = create_matrix(args)
    for line in matrix:
        print(line)

def compute_cos(args):
    args.pop(0)
    matrix = create_matrix(args)
    pass

def compute_sin(args):
    args.pop(0)
    matrix = create_matrix(args)
    pass

def compute_cosh(args):
    args.pop(0)
    matrix = create_matrix(args)
    pass

def compute_sinh(args):
    args.pop(0)
    matrix = create_matrix(args)
    pass